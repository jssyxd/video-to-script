{
  "framework": null,
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "uvicorn app.main:app --port 5000",
  "installCommand": "pip install -r requirements.txt",
  "regions": ["iad1"]
}